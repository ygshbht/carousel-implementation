import { Typography } from "@material-ui/core";

export default function Note() {
  return (
    <Typography id="functionality-note">
      (Note: Functionality of removing elements by swiping up/ down is not
      available in the NPM/ Github modules as it would not make sense for users
      to update carousel content on websites.)
    </Typography>
  );
}
